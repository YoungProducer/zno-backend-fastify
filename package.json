{
  "name": "zno-back-fastify",
  "version": "1.0.0",
  "main": "server/bin/app.js",
  "repository": "https://github.com/YoungProducer/zno-backend-fastify.git",
  "author": "YoungProducer <sashabezrukovownmail@gmail.com>",
  "license": "MIT",
  "scripts": {
    "install": "yarn --cwd ./client/package.json install && yarn --cwd ./server/package.json install",
    "client-dev": "yarn --cwd ./client start",
    "server-dev": "yarn --cwd ./server test-dev",
    "build-app": "yarn --cwd ./client build && yarn --cwd ./server build",
    "start-app": "yarn --cwd ./server start"
  }
}
